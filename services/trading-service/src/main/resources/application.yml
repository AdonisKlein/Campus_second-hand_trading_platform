server:
  port: 8083
spring:
  application:
    name: campus-trading-service
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
