server:
  port: 8080

spring:
  application:
    name: campus-api-gateway

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
