server:
  port: 8084
spring:
  application:
    name: campus-governance-service
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
