## 云原生实验与性能对比四人实施计划

## 一、目标与当前前置状态

完成路线图工作项 9—11：

- HPA 自动扩缩容实验。
- Trading → Marketplace 故障隔离实验。
- `midterm-check` 与 `microservices-end` 性能对比。
- 保存可复现脚本、原始结果、环境信息和现场演示证据。

当前 `main` 为 `211c7cf`，`midterm-check` 已存在，但尚未创建 `microservices-end`。开始正式实验前由成员 A：

1. 确认 main 分支工作项 1—8 的 CI 全绿。
2. 在该全绿提交创建不可移动的 annotated tag：`microservices-end`。
3. 记录 Docker Desktop CPU/内存、Kind/Kubernetes、MySQL、JDK、k6 和机器硬件版本。

本机没有 k6，因此统一使用固定版本的 Docker k6，不要求每个人单独安装。HPA 使用 `autoscaling/v2`、CPU request 和 Metrics Server；Kubernetes 只有取得资源指标并设置 CPU request 后才能正确计算 CPU 利用率。[Kubernetes HPA 文档](https://kubernetes.io/docs/concepts/workloads/autoscaling/horizontal-pod-autoscale/)

## 二、四人分工与协作顺序

| 成员                    | 主责                                                  | 分支                                | 主要文件归属                                                 |
| ----------------------- | ----------------------------------------------------- | ----------------------------------- | ------------------------------------------------------------ |
| A：实验基础与集成负责人 | 公共实验工具、Metrics Server、数据/报告格式、最终合并 | `codex/cloud-experiment-foundation` | `experiments/common/`、第三方工具清单、公共 PowerShell 脚本  |
| B：自动扩缩容负责人     | MinIO 图片 adapter、资源配置、HPA、扩缩容证据         | `codex/experiment-hpa`              | Marketplace 图片 module、HPA overlay、`experiments/hpa/`     |
| C：故障隔离负责人       | Trading 熔断、超时、重试、故障/恢复实验               | `codex/experiment-fault-isolation`  | Trading 远程调用 module、故障 overlay、`experiments/fault/`  |
| D：性能对比负责人       | 单体打包、双版本数据、k6 对比、统计结果               | `codex/experiment-performance`      | `experiments/performance/`、单体 benchmark overlay、结果汇总脚本 |

合并顺序：

1. A 的实验基础先合并到集成分支 `codex/cloud-native-experiments`。
2. B、C、D 从该提交建立各自分支并行工作。
3. 先合并 B、C，分别完成工作项 9、10。
4. D 在固定的 `midterm-check`、`microservices-end` 上完成工作项 11，不使用实验分支上未经标记的代码。
5. A 统一运行全套验证、更新路线图和工作日志，再合并 main。

四次独立提交：

- `experiment: establish reproducible cloud native lab harness`
- `experiment: demonstrate marketplace horizontal autoscaling`
- `experiment: demonstrate dependency fault isolation`
- `experiment: compare monolith and microservice performance`

## 三、成员 A：公共实验基础

建立统一命令 interface：

```powershell
powershell -File experiments/run.ps1 -Experiment hpa
powershell -File experiments/run.ps1 -Experiment fault
powershell -File experiments/run.ps1 -Experiment performance
```

实现内容：

- 固定 Metrics Server 版本和清单；Kind 环境增加 kubelet TLS 兼容 patch。
- 等待 `metrics.k8s.io` 可用后才开始实验，禁止用固定 sleep 代替 readiness。
- Docker 化运行固定版本 k6。
- 建立确定性数据生成器，同一随机种子生成单体和微服务两套逻辑等价 SQL。
- 建立资源采样器，每 5 秒记录：
  - Pod 副本数。
  - Pod/container CPU。
  - Pod/container 内存。
  - HPA current/target/desired replicas。
- 每次运行生成唯一 `runId`，记录提交 SHA、镜像 SHA、机器配置、Docker/Kind/Kubernetes 版本、测试参数和开始结束时间。
- 统一结果目录：

```text
artifacts/cloud-native/<runId>/
  environment.json
  manifests/
  k6-summary.json
  k6-console.txt
  resource-samples.csv
  pod-events.txt
  logs/
  result.json
```

- 小型 summary、CSV 和证据索引可以提交；完整压缩包作为 CI artifact/课程附件保存，并记录 SHA-256。
- k6 使用 `handleSummary()` 输出平均值、P95、吞吐和错误率；官方支持自定义 JSON summary 和原始时序输出。[Grafana k6 结果文档](https://grafana.com/docs/k6/latest/get-started/results-output/)

基础验收：

- 一条无压力 smoke 可生成完整机器可读结果。
- Metrics Server 可通过 `kubectl top pods` 返回数据。
- 任意实验失败时脚本退出码非 0，但仍保存已有日志和结果。
- 不提交 Secret、SMTP 密码或本地 `.env`。

## 四、成员 B：工作项 9——HPA 自动扩缩容

### 1. Marketplace 无状态化

沿用现有 `ProductImages` seam：

- 保留文件系统 adapter 供单元测试和简单本地开发使用。
- 新增 MinIO/S3 adapter，作为 HPA Kind overlay 默认 implementation。
- 上传、格式检查、图片规范化、5MB 限制和 URL 格式保持不变。
- 图片仍通过 `/api/media/product-images/**` 访问，前端和公开 interface 不改变。
- Marketplace Pod 不再挂载图片 PVC；MinIO 独占自己的 PVC。
- 固定 MinIO镜像版本，不使用 `latest`；课程实验使用最终社区安全修复版本，并明确不作为正式生产选型结论。

新增配置：

- `IMAGE_STORAGE=filesystem|minio`
- `MINIO_ENDPOINT`
- `MINIO_ACCESS_KEY`
- `MINIO_SECRET_KEY`
- `MINIO_BUCKET=campus-product-images`

### 2. 资源和 HPA

- Marketplace CPU request 初值 `200m`，limit `1000m`；内存 request `384Mi`，limit `768Mi`。
- `autoscaling/v2` HPA：
  - `minReplicas: 1`
  - `maxReplicas: 5`
  - CPU `averageUtilization: 60`
  - scale-up 稳定窗口 0 秒。
  - scale-down 稳定窗口 60 秒，便于课堂观察。
- 其他服务也设置 request/limit，避免 Marketplace 扩容挤垮基础设施。
- Metrics Server 固定使用与当前 Kind Kubernetes 兼容的版本；官方兼容表要求 0.8.x 支持 Kubernetes 1.31+，实施时以实际 Kind 节点版本校验。[Metrics Server 官方说明](https://github.com/kubernetes-sigs/metrics-server)

### 3. 压力实验

- 预置至少 20,000 件可见在售商品和卖家公开投影。
- 通过 Web/Gateway 的 `/api/search?scope=ITEMS...` 施压，不直接绕过 Gateway。
- 先 2 分钟预热，再分阶段从 20、50、100、150 VUs 加压。
- 运行期间同时执行：
  - `kubectl get hpa -w`
  - `kubectl get pods -w`
  - 资源采样器
- 停止压力后继续观察，直到副本恢复为 1。

验收：

- 空闲时 Marketplace 为 1 个 Pod。
- 压力升高后稳定增加到至少 2 个 Ready Pod。
- 压力下降后在设定稳定窗口后恢复到 1 个 Pod。
- 新 Pod 可以读取旧 Pod 上传的同一张 MinIO 图片。
- 结果中包含吞吐、平均/P95、错误率、CPU、内存和副本时间线。
- 若未触发扩容，实验判定失败，不得只提交静态 HPA YAML 或手动 scale 结果。

## 五、成员 C：工作项 10——故障隔离

### 1. 深化依赖调用 module

将 Trading 当前散落的 WebClient timeout/retry 收入统一 module：

```text
MarketplaceDependency.executeRead(operation)
```

该 interface 负责隐藏：

- 300ms 连接超时。
- 800ms 响应超时。
- 仅安全 GET 重试一次。
- Circuit Breaker 状态。
- 统一错误映射和 `Retry-After`。
- correlationId 日志传播。

使用显式版本的 Resilience4j Boot 4 module；2.4.0 已增加 Spring Boot 4 支持，避免依赖未包含 Boot 4 artifact 的旧 BOM配置。[Resilience4j 2.4.0 发布说明](https://github.com/resilience4j/resilience4j/releases)

熔断配置：

- count-based sliding window：10。
- minimum calls：5。
- failure rate threshold：50%。
- open duration：15 秒。
- half-open permitted calls：2。
- 仅网络失败、超时和 5xx 计入失败；业务 404/参数错误不计入。
- Circuit 状态不影响 Trading liveness/readiness，防止依赖故障导致 Kubernetes 重启正常服务。

公开行为：

- 创建购买意向依赖 Marketplace 失败时返回：
  - HTTP `503`
  - code：`PRODUCT_SERVICE_UNAVAILABLE`
  - 固定安全提示。
  - `Retry-After: 1`
- 不返回伪造商品。
- 不创建订单、不写 Outbox、不改变商品。
- 已有订单查询仍能从 Trading 自己的数据库返回。

### 2. 故障实验

自动脚本：

1. 创建测试买卖双方、商品和一个已有订单。
2. 记录 Trading 数据库订单数。
3. 将 Marketplace Deployment scale 到 0。
4. 连续请求创建新购买意向，使熔断器由 CLOSED 进入 OPEN。
5. 验证稳定返回预定 503，而不是超时、500 或半条订单。
6. 验证 Account、Governance 健康，Trading 既有订单查询仍成功。
7. 恢复 Marketplace，等待 Ready。
8. 等待 15 秒并发送探测请求，展示 HALF_OPEN → CLOSED。
9. 验证恢复后新的购买意向可以正常创建。

状态变化通过结构化日志和指标记录，不新增公开管理接口。

验收：

- Marketplace 停止时，其他服务 Pod 均保持 Ready。
- 新购买意向全部没有数据库副作用。
- 故障响应时间有明确上限，且返回设计好的 503。
- 日志能看到 CLOSED → OPEN → HALF_OPEN → CLOSED。
- 恢复不要求重启 Trading。
- 单元测试覆盖超时、5xx、404、不重试非幂等操作、熔断打开和恢复。
- 集成测试覆盖数据库最终状态及 correlationId。

## 六、成员 D：工作项 11——单体与微服务性能对比

### 1. 固定两个版本

- 单体：不可移动的 `midterm-check`。
- 微服务：工作项 1—8 全绿后创建的 `microservices-end`。
- 脚本通过 `git archive` 解出单体到临时目录，用实验专用 Dockerfile 构建；不修改历史 tag。
- 两个环境顺序运行，不能同时占用机器资源。
- 固定 MySQL 8.4、Kind版本和 Docker Desktop 资源设置。

### 2. 同一批逻辑数据

固定随机种子生成：

- 2,500 个用户。
- 20,000 件商品。
- 50,000 条公开留言。
- 固定被查询的商品 ID 集合。
- 图片 URL 为空，避免网络和对象存储影响三个查询接口。

因数据库结构不同，生成两份 SQL，但实体 ID、标题、价格、卖家和留言内容保持一致。导入后脚本必须核对行数和校验和。

### 3. 对比接口和运行方式

接口保持路线图确定的三条：

- `GET /api/items`
- `GET /api/items/{id}`
- `GET /api/messages/item/{itemId}`

每个 k6 iteration 依次访问三条接口并分别打 tag，结果同时给出单接口和整体指标。

参数：

- 并发档位：10、50、100 VUs。
- 每种架构、每个档位运行 3 次。
- 每次预热 2 分钟，正式测量 5 分钟。
- 两次运行间冷却 2 分钟。
- 总计 18 次正式运行。

性能对比期间：

- 禁用 HPA，所有应用 Deployment 固定 1 副本。
- 禁止 DEBUG/SQL 日志。
- 每次使用相同 JVM 参数。
- 单体后端总 CPU/内存 limit 与微服务 Gateway+四个业务服务的总 limit 相等。
- MySQL、Web 和负载机配置保持一致。
- 分别统计应用层总 CPU/内存，也单独列出 MySQL、Redis、RabbitMQ开销。

### 4. 数据汇总

每次保留：

- 请求数与吞吐量。
- 平均响应时间。
- P95。
- 错误率。
- 各 Pod CPU/内存采样。
- 镜像 SHA、Git SHA、数据校验和。
- k6 原始 summary 和控制台输出。

最终表格对三个重复结果分别列出，再以中位数作为主对比值，同时保留最小/最大值。不得只保留“最好的一次”。

验收：

- 18 次运行全部有独立原始结果。
- 两个版本数据量和固定样本校验一致。
- 脚本能够从空环境重复完成部署、导入、预热、压测和采样。
- 微服务较慢时照实记录，并结合 Gateway、序列化、跨进程调用、多 JVM 与 Redis/RabbitMQ开销解释。
- 在数据产生前不得出现“性能提升”结论。

## 七、最终联合验收和现场展示

A 负责按以下顺序组织现场演示：

1. 展示 `midterm-check` 和 `microservices-end` 对应的完整 SHA。
2. 一条命令启动 HPA 实验。
3. `kubectl get hpa,pods -w` 展示 1 → 多 Pod → 1。
4. 展示同一 MinIO 图片可被多个 Marketplace Pod读取。
5. 停止 Marketplace，展示 Trading 固定 503 和订单数不变。
6. 展示其他服务仍 Ready。
7. 恢复 Marketplace，展示熔断器恢复且无需重启 Trading。
8. 打开性能对比汇总，并能追溯到18份原始结果。
9. 展示机器配置、镜像 SHA、数据校验和、CPU/内存采样。
10. 运行实验脚本静态验证和核心测试。

最终门禁：

- 五个服务分别 `mvn verify`。
- Marketplace MinIO adapter 集成测试通过。
- Trading 熔断与无副作用测试通过。
- HPA、故障、性能结果 Schema 校验通过。
- `git diff --check` 通过。
- 无 Secret 或真实账号进入 Git。
- 路线图和 `docs/ai/WORKLOG.md` 记录每项提交号、运行环境、实际结果和遗留问题。